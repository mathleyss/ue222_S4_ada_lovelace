{% extends 'base.html.twig' %}

{% block title %}{{article.title}} - Edition{% endblock %}

{% block body %}
    <h1>Edition de : {{article.title}}</h1>

    {{ include('article/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('article_index') }}" class="action-link">Retour à l'index</a>

    {{ include('article/_delete_form.html.twig') }}
{% endblock %}
