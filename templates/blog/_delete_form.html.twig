<form method="post" action="{{ path('app_blog_delete', {'id': blog.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce blog ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blog.id) }}">
    <button class="btn btn-danger" style="font-family: 'Montserrat', sans-serif;">Supprimer</button>
</form>
