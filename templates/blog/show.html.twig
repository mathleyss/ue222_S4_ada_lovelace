{% extends 'base.html.twig' %}

{% block title %}{{ blog.titre }}{% endblock %}

{% block body %}
    <h1>{{ blog.titre }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ blog.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ blog.titre }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ blog.texte }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ blog.prenom }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ blog.nom }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ blog.date ? blog.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_blog_index') }}" class="action-link">Retour à l'index</a>

    <a href="{{ path('app_blog_edit', {'id': blog.id}) }}" class="action-link">Modifier</a>

    {{ include('blog/_delete_form.html.twig') }}
{% endblock %}
