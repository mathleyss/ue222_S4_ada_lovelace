{% extends 'base.html.twig' %}

{% block title %}{{ category.name }} - Edition{% endblock %}

{% block body %}
    <h1>Edition de : {{ category.name }}</h1>

    {{ include('category/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('category_index') }}" class="action-link">Retour à l'index</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
